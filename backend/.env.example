# ==================== DATABASE CONFIGURATION ====================
# PostgreSQL connection string: postgresql://username:password@host:port/database
# Example: postgresql://jobtracker:password@localhost:5432/job_tracker
# For production, use environment-managed credentials (AWS RDS, GCP Cloud SQL, etc.)
# See config.py for default values
DATABASE_URL=postgresql://jobtracker:password@localhost:5432/job_tracker

# ==================== JWT SECURITY CONFIGURATION ====================
# Secret key for signing JWT tokens
# CRITICAL: Change this to a random strong string in production (minimum 32 characters)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# WARNING: If changed, all existing JWT tokens become invalid
SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars

# JWT algorithm for token signing/verification
# HS256 (HMAC with SHA-256): Uses symmetric key (same key for both operations)
# DO NOT CHANGE unless you understand the implications
ALGORITHM=HS256

# JWT token expiry time in minutes
# Shorter expiry = more security (less window for token theft)
# Longer expiry = better UX (less frequent re-authentication)
# Typical: 15-60 minutes depending on security requirements
# Default: 30 minutes (balanced for development)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== EMAIL SETTINGS (OPTIONAL) ====================
# For future notification features: password reset, application alerts, etc.
# Leave blank to disable email features
# 
# Gmail setup example:
# 1. Create an App Password (if 2FA enabled) or enable "Less secure app access"
# 2. Use Gmail address as SMTP_USER
# 3. Use app password as SMTP_PASSWORD
#
# Other email providers:
# - SendGrid: SMTP_HOST=smtp.sendgrid.net, SMTP_PORT=587
# - AWS SES: SMTP_HOST=email-smtp.{region}.amazonaws.com, SMTP_PORT=587
# - Office365: SMTP_HOST=smtp.office365.com, SMTP_PORT=587

# SMTP server hostname (email provider's SMTP endpoint)
SMTP_HOST=smtp.gmail.com

# SMTP server port
# 25: Unencrypted (rarely used, often blocked)
# 587: TLS/StartTLS (recommended, starts unencrypted then upgrades)
# 465: SSL/Implicit TLS (encrypted from start)
SMTP_PORT=587

# Email account credentials
# SMTP_USER: Full email address (for Gmail: your-email@gmail.com)
SMTP_USER=your-email@gmail.com

# SMTP_PASSWORD: Account password or app-specific password
# For Gmail with 2FA: Use 16-character app password, not account password
SMTP_PASSWORD=your-app-password